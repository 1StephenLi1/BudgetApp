<!DOCTYPE html>
<html>
<head>
    <% include includes/head.ejs %>
</head>

<body class="hold-transition login-page">
        <div class="login-box">                
        <!-- /.login-logo -->
        <div class="login-box-body">
                <% if (forgot != null && forgot.length > 0) { %>
                    <div class="alert alert-danger"><%= forgot %></div>
                <% } %>
                <% if (reset != null && reset.length > 0) { %>
                    <div class="alert alert-success"><%= reset %></div>
                <% } %>
            <p class="login-box-msg">Enter your e-mail to reset your password</p>
            <form method="post">
                <div class="form-group has-feedback">
                    <input type="text" name="email" class="form-control" placeholder="Email" required autofocus>
                    <span class="glyphicon glyphicon-envelope form-control-feedback"></span>
                </div>
                <div class="row">
                    <!-- /.col -->
                    <div class="col-xs-offset-4 col-xs-4">
                        <button type="submit" class="btn btn-primary btn-block btn-flat">Reset</button>
                </div>
            </div>
        </form>
</div>
    </div>
    <!-- /.login-box-body -->
    <% include includes/jsDefaults.ejs %>

</body>
</html>
